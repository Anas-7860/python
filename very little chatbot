name = input("what is your name ? ")
if name == "aadi" or name == "zishan" or name == "Aadi" or name == "Zishan":
  evil_status = input("Are you evil ? ")
  if evil_status == "Yes" or "yes" or "YES":
    Gdeed_count = int(input("How many good deeds have you done today ? "))
    if Gdeed_count < 4:
      print(name+" Jaldi waha se hato......!")
      exit()
    else:  print("you can come in "+name+" you are now good !")
    
print(name+" if you want to order something , here is the menu :" )
print("MENU :-\nsamosa\nidli\ndosa\nburger\neggroll")
order = input("Write your order here : ")
Address = input("write your address here : ")
quantity = input("how many of "+order+" do you like :")
price = 4;
total = price * int(quantity)
print("Thank you ! "+name+"\n"+"your order , which is "+order+" is confirmed\n"+"your order will arrive on "+Address)
print("you need to pay : "+str(total)+"$")
